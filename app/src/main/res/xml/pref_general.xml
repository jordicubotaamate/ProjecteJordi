<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <SwitchPreference
        android:defaultValue="true"
        android:key="negatiu"
        android:summary="@string/negatiuText"
        android:title="@string/negatiu" />

    <SwitchPreference
        android:defaultValue="false"
        android:key="decimals"
        android:summary="@string/decimalsText"
        android:title="@string/decimals" />

    <ListPreference
        android:defaultValue="-1"
        android:entries="@array/moneda"
        android:entryValues="@array/moneda"
        android:key="moneda"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_add_friends_to_messages" />

</PreferenceScreen>
